# SellPilot Development Phases

> **Complete Project Execution Roadmap**  
> This document defines the entire development lifecycle of SellPilot, from initial planning through production deployment. Each phase includes clear objectives, technical specifications, acceptance criteria, and quality gates.

**Version:** 1.0  
**Last Updated:** February 2026  
**Status:** Active Development

---

## Table of Contents

- [Overview](#overview)
- [Development Principles](#development-principles)
- [Phase 0: Planning & Architecture](#phase-0-planning--architecture)
- [Phase 1: Backend Foundation](#phase-1-backend-foundation)
- [Phase 2: Ecommerce Core](#phase-2-ecommerce-core)
- [Phase 3: Cloud Infrastructure](#phase-3-cloud-infrastructure)
- [Phase 4: Frontend Dashboard](#phase-4-frontend-dashboard)
- [Phase 5: AI Marketing Studio](#phase-5-ai-marketing-studio)
- [Phase 6: Growth Agent](#phase-6-growth-agent)
- [Phase 7: Automation Engine](#phase-7-automation-engine)
- [Phase 8: Production Deployment](#phase-8-production-deployment)
- [Appendix](#appendix)

---

## Overview

SellPilot's development follows an **eight-phase iterative approach**, where each phase builds upon the previous one while maintaining system stability. This structure ensures:

- **Incremental Value Delivery**: Each phase produces working, testable features
- **Risk Mitigation**: Critical architecture decisions made early
- **Quality Gates**: No phase begins until previous phase acceptance criteria are met
- **Production Mindset**: Every phase considers scalability, security, and maintainability

### Phase Summary

| Phase | Focus Area | Duration | Complexity | Dependencies |
|-------|------------|----------|------------|--------------|
| 0 | Planning & Architecture | 1 week | Low | None |
| 1 | Authentication & Multi-Tenancy | 2-3 weeks | Medium | Phase 0 |
| 2 | Ecommerce Core Backend | 3-4 weeks | High | Phase 1 |
| 3 | AWS Cloud Integration | 2 weeks | Medium | Phase 2 |
| 4 | Frontend Dashboard | 4-5 weeks | High | Phase 3 |
| 5 | AI Marketing Studio | 2-3 weeks | Medium | Phase 4 |
| 6 | Seller Growth Agent | 3 weeks | High | Phase 5 |
| 7 | Automation Engine | 3 weeks | High | Phase 6 |
| 8 | CI/CD & Production Polish | 3 weeks | Medium | Phase 7 |

**Total Estimated Duration:** 23-28 weeks (5.5-7 months)

---

## Development Principles

All development across all phases adheres to these core principles:

### 1. Security First
- Multi-tenant isolation enforced at every layer
- No data leakage between stores
- RBAC validated before every operation
- All inputs validated and sanitized
- Audit logging for critical operations

### 2. Scalability by Design
- Database queries optimized from day one
- Async processing for heavy operations
- Stateless API design for horizontal scaling
- Caching strategies implemented early

### 3. Production Quality
- Code follows established style guides
- Test coverage maintained above 80%
- Documentation updated with every change
- No commented-out code in production
- Environment-specific configurations

### 4. Developer Experience
- Clear error messages and logging
- Comprehensive API documentation
- Consistent naming conventions
- Modular, reusable components

### 5. Business Value
- Every feature solves a real seller problem
- Technical decisions aligned with product vision
- Performance metrics tracked and improved
- User feedback incorporated iteratively

---

## Phase 0: Planning & Architecture

**Status:** ✅ Completed  
**Duration:** 1 week  
**Complexity:** Low

### Objective

Establish the complete technical blueprint for SellPilot before writing any production code. This phase ensures architectural consistency, prevents costly refactors, and creates a shared understanding of system design across the entire project.

### Why This Phase Matters

Most failed projects skip proper planning. Phase 0 prevents:
- Architectural inconsistencies that emerge mid-development
- Database schema changes that require complex migrations
- API design conflicts between frontend and backend teams
- Security vulnerabilities from unclear access control models
- Scope creep from undefined requirements

### Core Deliverables

#### 1. Repository Structure

Create the foundational repository architecture:

```bash
sellpilot/
├── backend/
│   ├── config/                 # Django project settings
│   │   ├── settings/
│   │   │   ├── __init__.py
│   │   │   ├── base.py        # Shared settings
│   │   │   ├── development.py # Local dev settings
│   │   │   ├── staging.py     # Staging environment
│   │   │   └── production.py  # Production settings
│   │   ├── urls.py
│   │   ├── wsgi.py
│   │   └── asgi.py
│   │
│   ├── apps/
│   │   ├── accounts/          # User authentication
│   │   ├── stores/            # Multi-tenant store management
│   │   ├── products/          # Product catalog
│   │   ├── orders/            # Order processing
│   │   ├── customers/         # Customer management
│   │   ├── analytics/         # Metrics and reporting
│   │   ├── marketing_ai/      # AI content generation
│   │   ├── growth_agent/      # Intelligent recommendations
│   │   ├── automation/        # Workflow engine
│   │   └── invoices/          # Invoice generation
│   │
│   ├── core/                  # Shared utilities
│   │   ├── middleware/
│   │   ├── permissions/
│   │   ├── exceptions/
│   │   ├── validators/
│   │   └── utils/
│   │
│   ├── tests/
│   │   ├── unit/
│   │   ├── integration/
│   │   └── fixtures/
│   │
│   ├── requirements/
│   │   ├── base.txt
│   │   ├── development.txt
│   │   ├── production.txt
│   │   └── testing.txt
│   │
│   ├── manage.py
│   ├── pytest.ini
│   ├── .env.example
│   └── Dockerfile
│
├── frontend/
│   ├── src/
│   │   ├── app/               # Next.js App Router
│   │   │   ├── (auth)/        # Auth pages group
│   │   │   ├── (dashboard)/   # Dashboard pages group
│   │   │   ├── api/           # API routes
│   │   │   └── layout.tsx
│   │   │
│   │   ├── components/
│   │   │   ├── ui/            # Base UI components
│   │   │   ├── forms/         # Form components
│   │   │   ├── layouts/       # Layout components
│   │   │   └── charts/        # Chart components
│   │   │
│   │   ├── lib/
│   │   │   ├── api/           # API client
│   │   │   ├── hooks/         # Custom React hooks
│   │   │   ├── utils/         # Utility functions
│   │   │   └── types/         # TypeScript types
│   │   │
│   │   └── store/             # State management
│   │
│   ├── public/
│   ├── package.json
│   ├── tsconfig.json
│   ├── next.config.js
│   ├── tailwind.config.js
│   └── Dockerfile
│
├── infrastructure/
│   ├── terraform/             # Infrastructure as Code
│   │   ├── modules/
│   │   ├── environments/
│   │   │   ├── development/
│   │   │   ├── staging/
│   │   │   └── production/
│   │   └── main.tf
│   │
│   ├── docker/
│   │   ├── nginx/
│   │   │   ├── nginx.conf
│   │   │   └── Dockerfile
│   │   └── scripts/
│   │
│   └── kubernetes/            # Future: K8s configs
│
├── docs/
│   ├── architecture/
│   │   ├── system-design.md
│   │   ├── multi-tenancy.md
│   │   ├── security-model.md
│   │   └── diagrams/
│   │
│   ├── api/
│   │   ├── authentication.md
│   │   ├── endpoints.md
│   │   └── examples.md
│   │
│   ├── database/
│   │   ├── schema.md
│   │   ├── migrations.md
│   │   └── erd.md
│   │
│   ├── deployment/
│   │   ├── aws-setup.md
│   │   ├── docker.md
│   │   └── ci-cd.md
│   │
│   └── phases/
│       └── PHASES.md          # This document
│
├── scripts/
│   ├── setup/
│   │   ├── init_database.sh
│   │   └── create_superuser.sh
│   │
│   ├── deployment/
│   │   ├── deploy_staging.sh
│   │   └── deploy_production.sh
│   │
│   └── utilities/
│       ├── seed_data.py
│       └── backup_db.sh
│
├── .github/
│   ├── workflows/
│   │   ├── backend-ci.yml
│   │   ├── frontend-ci.yml
│   │   ├── deploy-staging.yml
│   │   └── deploy-production.yml
│   │
│   ├── ISSUE_TEMPLATE/
│   ├── PULL_REQUEST_TEMPLATE.md
│   └── CODEOWNERS
│
├── docker-compose.yml
├── docker-compose.prod.yml
├── .gitignore
├── .env.example
├── README.md
├── LICENSE
└── CONTRIBUTING.md
```

**Why This Structure:**
- Clear separation of concerns (backend/frontend/infrastructure)
- Environment-specific configurations prevent production accidents
- Tests organized by type for clear test strategy
- Documentation co-located with code for accuracy
- Infrastructure as Code for reproducible deployments

#### 2. Architecture Documentation

**File:** `docs/architecture/system-design.md`

Must define:

**System Components:**
- Client tier (browser, mobile web)
- Application tier (Next.js frontend)
- API tier (Django REST Framework)
- Data tier (PostgreSQL)
- Cache tier (Redis)
- Task queue (Celery)
- Storage tier (S3)
- CDN tier (CloudFront)

**Communication Protocols:**
- Client ↔ Frontend: HTTPS, WebSocket (future)
- Frontend ↔ API: REST over HTTPS, JWT auth
- API ↔ Database: PostgreSQL protocol
- API ↔ Cache: Redis protocol
- API ↔ Task Queue: AMQP/Redis

**Data Flow Examples:**
1. User uploads product image → Frontend → API → S3 → Database record
2. Customer places order → API → Database → Celery task → Email notification
3. Seller requests analytics → API → Redis cache check → Database query → Response

**Scaling Strategy:**
- Horizontal scaling of API servers behind load balancer
- Read replicas for database
- Redis cluster for cache and queue
- CDN for static assets
- Celery workers scaled independently

#### 3. Database Schema Design

**File:** `docs/database/schema.md`

Must include complete schema definitions with:

**Core Principles:**
- All tables use UUIDs for primary keys (security, distribution)
- Timestamps on every table (created_at, updated_at)
- Soft deletes where data retention matters
- Foreign key constraints enforced
- Indexes on frequently queried fields

**Schema Evolution Strategy:**
- All schema changes via migrations
- Migrations are reversible
- No data loss migrations
- Backward compatibility for 1 version

**Example Core Schema:**

```sql
-- Users table
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    full_name VARCHAR(200),
    is_active BOOLEAN DEFAULT true,
    is_staff BOOLEAN DEFAULT false,
    email_verified BOOLEAN DEFAULT false,
    last_login TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_active ON users(is_active) WHERE is_active = true;

-- Stores table (multi-tenant core)
CREATE TABLE stores (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    owner_id UUID NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
    name VARCHAR(200) NOT NULL,
    slug VARCHAR(200) UNIQUE NOT NULL,
    description TEXT,
    industry VARCHAR(100),
    currency VARCHAR(3) DEFAULT 'USD',
    timezone VARCHAR(50) DEFAULT 'UTC',
    settings JSONB DEFAULT '{}',
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_stores_owner ON stores(owner_id);
CREATE INDEX idx_stores_slug ON stores(slug);
CREATE INDEX idx_stores_active ON stores(is_active) WHERE is_active = true;

-- Store members (RBAC)
CREATE TABLE store_members (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    store_id UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    role VARCHAR(20) NOT NULL CHECK (role IN ('owner', 'staff', 'viewer')),
    permissions JSONB DEFAULT '{}',
    invited_by UUID REFERENCES users(id),
    joined_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE(store_id, user_id)
);

CREATE INDEX idx_store_members_store ON store_members(store_id);
CREATE INDEX idx_store_members_user ON store_members(user_id);
CREATE INDEX idx_store_members_role ON store_members(store_id, role);
```

Full schema must be documented for all 30+ tables before Phase 1 begins.

#### 4. API Design Specification

**File:** `docs/api/endpoints.md`

Must define:

**API Versioning Strategy:**
- All endpoints prefixed with `/api/v1/`
- Version in URL path, not header
- Backward compatibility for minor versions
- Breaking changes require new major version

**Authentication Flow:**
```
1. POST /api/v1/auth/register/
   → {email, password, name}
   ← {user, message}

2. POST /api/v1/auth/login/
   → {email, password}
   ← {access, refresh, user}

3. All subsequent requests:
   Header: Authorization: Bearer {access_token}

4. POST /api/v1/auth/refresh/
   → {refresh}
   ← {access}
```

**Store-Scoped Resource Pattern:**
All ecommerce resources follow this URL pattern:
```
/api/v1/stores/{store_id}/{resource}/
```

This enforces multi-tenant isolation at the routing level.

**Standard Response Format:**
```json
{
  "data": { /* actual payload */ },
  "meta": {
    "pagination": { /* if applicable */ },
    "timestamp": "2026-02-12T14:30:00Z"
  },
  "errors": [ /* if applicable */ ]
}
```

**Error Response Format:**
```json
{
  "errors": [
    {
      "code": "VALIDATION_ERROR",
      "message": "Invalid email format",
      "field": "email"
    }
  ],
  "meta": {
    "timestamp": "2026-02-12T14:30:00Z",
    "request_id": "req_abc123"
  }
}
```

#### 5. Multi-Tenancy Security Model

**File:** `docs/architecture/multi-tenancy.md`

Must define:

**Tenant Isolation Strategy:**

SellPilot uses **row-level multi-tenancy** where:
- Single database shared by all tenants
- Every row tagged with store_id
- Application enforces isolation via ORM filters
- Database foreign keys prevent orphaned data

**Why Row-Level vs Schema-Level:**

| Approach | Pros | Cons | Decision |
|----------|------|------|----------|
| Separate DB per tenant | Maximum isolation | Expensive, complex migrations | ❌ Rejected |
| Schema per tenant | Good isolation | Still complex operations | ❌ Rejected |
| Row-level isolation | Simple, scalable | Requires careful validation | ✅ **Chosen** |

**Security Enforcement Layers:**

1. **Middleware Layer:**
   ```python
   class StoreContextMiddleware:
       def __call__(self, request):
           if request.user.is_authenticated:
               store_id = request.headers.get('X-Store-ID')
               if store_id:
                   # Validate user has access to this store
                   membership = StoreMember.objects.filter(
                       store_id=store_id,
                       user_id=request.user.id
                   ).first()
                   
                   if membership:
                       request.store = membership.store
                       request.store_role = membership.role
   ```

2. **ORM Query Layer:**
   ```python
   class StoreOwnedQuerySet(models.QuerySet):
       def for_store(self, store):
           return self.filter(store=store)
   
   class StoreOwnedModel(models.Model):
       store = models.ForeignKey(Store, on_delete=models.CASCADE)
       objects = StoreOwnedQuerySet.as_manager()
       
       class Meta:
           abstract = True
   ```

3. **Permission Layer:**
   ```python
   class IsStoreOwner(permissions.BasePermission):
       def has_permission(self, request, view):
           return (
               hasattr(request, 'store') and
               request.store_role == 'owner'
           )
   ```

4. **Serializer Layer:**
   ```python
   class ProductSerializer(serializers.ModelSerializer):
       def validate(self, attrs):
           # Ensure store_id matches request context
           if 'store' in attrs:
               if attrs['store'] != self.context['request'].store:
                   raise ValidationError("Cannot create product for different store")
           return attrs
   ```

**Audit Requirements:**

Every mutation (create, update, delete) must log:
- Who (user_id)
- What (action type)
- When (timestamp)
- Where (store_id, resource_id)
- Changes (old_value → new_value)

#### 6. Development Standards

**File:** `docs/development/standards.md`

**Python/Django Standards:**
- Follow PEP 8 style guide
- Use Black for formatting (line length: 100)
- Use isort for import sorting
- Type hints for function signatures
- Docstrings for public methods
- No business logic in views (use services)
- Fat models, thin views, smart services

**JavaScript/TypeScript Standards:**
- ESLint + Prettier configuration
- Functional components only (no class components)
- Custom hooks for reusable logic
- Proper TypeScript types (no `any`)
- Props interfaces for components
- Descriptive variable names

**Git Workflow:**
- Feature branches from `develop`
- Branch naming: `feature/`, `bugfix/`, `hotfix/`
- Commit messages: Conventional Commits format
- Pull requests require 1 approval
- Squash and merge to keep history clean

**Testing Requirements:**
- Unit test coverage > 80%
- Integration tests for critical flows
- E2E tests for user journeys
- Tests run in CI before merge
- No tests should depend on external services

### Acceptance Criteria

Phase 0 is complete when:

- [ ] Repository structure created and pushed to GitHub
- [ ] All documentation files created with content
- [ ] Database schema v1 documented for all core tables
- [ ] API endpoint specifications documented
- [ ] Multi-tenancy security model documented
- [ ] Development standards documented
- [ ] Project roadmap committed
- [ ] Team alignment on architecture achieved
- [ ] Phase 1 development can begin immediately

### Deliverable Checklist

- [ ] `README.md` - Project overview and setup instructions
- [ ] `docs/architecture/system-design.md` - Complete system architecture
- [ ] `docs/database/schema.md` - Full database schema
- [ ] `docs/api/endpoints.md` - API specification v1
- [ ] `docs/architecture/multi-tenancy.md` - Security model
- [ ] `docs/development/standards.md` - Coding standards
- [ ] `docs/phases/PHASES.md` - This document
- [ ] `.env.example` - Environment variable template
- [ ] `docker-compose.yml` - Local development setup
- [ ] GitHub Project board created

---

## Phase 1: Backend Foundation

**Status:** 🚧 In Progress  
**Duration:** 2-3 weeks  
**Complexity:** Medium  
**Dependencies:** Phase 0 complete

### Objective

Build the foundational SaaS backend infrastructure including user authentication, JWT token management, multi-tenant store model, and role-based access control. This phase establishes the security and architectural patterns that all future phases will build upon.

### Why This Phase Matters

Without proper authentication and multi-tenancy, building ecommerce features would require significant refactoring later. This phase ensures:
- Secure user authentication from day one
- Multi-tenant isolation built into the foundation
- RBAC ready for team collaboration features
- Scalable architecture patterns established

### Technical Scope

#### 1. Project Initialization

**Set up Django project with production-ready structure:**

```bash
# Create virtual environment
python -m venv venv
source venv/bin/activate

# Install Django and core dependencies
pip install django==4.2
pip install djangorestframework==3.14
pip install djangorestframework-simplejwt==5.3
pip install python-decouple==3.8
pip install psycopg2-binary==2.9
pip install django-cors-headers==4.3
pip install drf-spectacular==0.27

# Initialize Django project
django-admin startproject config .
```

**Configure settings structure:**

```python
# config/settings/__init__.py
from decouple import config

ENV = config('DJANGO_ENV', default='development')

if ENV == 'production':
    from .production import *
elif ENV == 'staging':
    from .staging import *
else:
    from .development import *
```

**Base settings** (`config/settings/base.py`):

```python
import os
from pathlib import Path
from decouple import config

BASE_DIR = Path(__file__).resolve().parent.parent.parent

SECRET_KEY = config('SECRET_KEY')
DEBUG = config('DEBUG', default=False, cast=bool)
ALLOWED_HOSTS = config('ALLOWED_HOSTS', default='localhost').split(',')

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    
    # Third party
    'rest_framework',
    'rest_framework_simplejwt',
    'corsheaders',
    'drf_spectacular',
    
    # Local apps
    'apps.accounts',
    'apps.stores',
    'core',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'corsheaders.middleware.CorsMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'core.middleware.StoreContextMiddleware',  # Custom middleware
]

ROOT_URLCONF = 'config.urls'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': config('DB_NAME'),
        'USER': config('DB_USER'),
        'PASSWORD': config('DB_PASSWORD'),
        'HOST': config('DB_HOST', default='localhost'),
        'PORT': config('DB_PORT', default='5432'),
    }
}

AUTH_USER_MODEL = 'accounts.User'

REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ],
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticated',
    ],
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 20,
    'DEFAULT_SCHEMA_CLASS': 'drf_spectacular.openapi.AutoSchema',
}

from datetime import timedelta

SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(minutes=15),
    'REFRESH_TOKEN_LIFETIME': timedelta(days=7),
    'ROTATE_REFRESH_TOKENS': True,
    'BLACKLIST_AFTER_ROTATION': True,
}
```

#### 2. Accounts Module (Authentication)

**Create custom User model:**

```python
# apps/accounts/models.py
import uuid
from django.contrib.auth.models.models import AbstractBaseUser, BaseUserManager, PermissionsMixin
from django.db import models
from django.utils import timezone

class UserManager(BaseUserManager):
    def create_user(self, email, password=None, **extra_fields):
        if not email:
            raise ValueError('Email is required')
        
        email = self.normalize_email(email)
        user = self.model(email=email, **extra_fields)
        user.set_password(password)
        user.save(using=self._db)
        return user
    
    def create_superuser(self, email, password=None, **extra_fields):
        extra_fields.setdefault('is_staff', True)
        extra_fields.setdefault('is_superuser', True)
        
        return self.create_user(email, password, **extra_fields)

class User(AbstractBaseUser, PermissionsMixin):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    email = models.EmailField(unique=True, max_length=255)
    full_name = models.CharField(max_length=200, blank=True)
    
    is_active = models.BooleanField(default=True)
    is_staff = models.BooleanField(default=False)
    
    email_verified = models.BooleanField(default=False)
    last_login = models.DateTimeField(null=True, blank=True)
    
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    objects = UserManager()
    
    USERNAME_FIELD = 'email'
    REQUIRED_FIELDS = ['full_name']
    
    class Meta:
        db_table = 'users'
        verbose_name = 'User'
        verbose_name_plural = 'Users'
    
    def __str__(self):
        return self.email
```

**Authentication Serializers:**

```python
# apps/accounts/serializers.py
from rest_framework import serializers
from django.contrib.auth import get_user_model
from django.contrib.auth.password_validation import validate_password

User = get_user_model()

class UserRegistrationSerializer(serializers.ModelSerializer):
    password = serializers.CharField(write_only=True, validators=[validate_password])
    password_confirm = serializers.CharField(write_only=True)
    
    class Meta:
        model = User
        fields = ['email', 'full_name', 'password', 'password_confirm']
    
    def validate(self, attrs):
        if attrs['password'] != attrs['password_confirm']:
            raise serializers.ValidationError({"password": "Passwords don't match"})
        return attrs
    
    def create(self, validated_data):
        validated_data.pop('password_confirm')
        user = User.objects.create_user(**validated_data)
        return user

class UserSerializer(serializers.ModelSerializer):
    class Meta:
        model = User
        fields = ['id', 'email', 'full_name', 'is_active', 'created_at']
        read_only_fields = ['id', 'created_at']
```

**Authentication Views:**

```python
# apps/accounts/views.py
from rest_framework import status, generics
from rest_framework.response import Response
from rest_framework.permissions import AllowAny, IsAuthenticated
from rest_framework_simplejwt.views import TokenObtainPairView
from .serializers import UserRegistrationSerializer, UserSerializer

class RegisterView(generics.CreateAPIView):
    permission_classes = [AllowAny]
    serializer_class = UserRegistrationSerializer
    
    def create(self, request, *args, **kwargs):
        serializer = self.get_serializer(data=request.data)
        serializer.is_valid(raise_exception=True)
        user = serializer.save()
        
        return Response({
            'data': {
                'user': UserSerializer(user).data,
                'message': 'Registration successful. Please log in.'
            }
        }, status=status.HTTP_201_CREATED)

class CurrentUserView(generics.RetrieveAPIView):
    permission_classes = [IsAuthenticated]
    serializer_class = UserSerializer
    
    def get_object(self):
        return self.request.user
```

**URLs Configuration:**

```python
# apps/accounts/urls.py
from django.urls import path
from rest_framework_simplejwt.views import TokenObtainPairView, TokenRefreshView
from .views import RegisterView, CurrentUserView

urlpatterns = [
    path('register/', RegisterView.as_view(), name='register'),
    path('login/', TokenObtainPairView.as_view(), name='login'),
    path('refresh/', TokenRefreshView.as_view(), name='token_refresh'),
    path('me/', CurrentUserView.as_view(), name='current_user'),
]
```

#### 3. Stores Module (Multi-Tenancy Core)

**Store Models:**

```python
# apps/stores/models.py
import uuid
from django.db import models
from django.conf import settings
from django.utils.text import slugify

class Store(models.Model):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    owner = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.RESTRICT,  # Prevent deleting user with stores
        related_name='owned_stores'
    )
    
    name = models.CharField(max_length=200)
    slug = models.SlugField(max_length=200, unique=True)
    description = models.TextField(blank=True)
    
    industry = models.CharField(max_length=100, blank=True)
    currency = models.CharField(max_length=3, default='USD')
    timezone = models.CharField(max_length=50, default='UTC')
    
    settings = models.JSONField(default=dict, blank=True)
    is_active = models.BooleanField(default=True)
    
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        db_table = 'stores'
        ordering = ['-created_at']
    
    def __str__(self):
        return self.name
    
    def save(self, *args, **kwargs):
        if not self.slug:
            self.slug = slugify(self.name)
        super().save(*args, **kwargs)
    
    def has_member(self, user):
        """Check if user is a member of this store"""
        return self.members.filter(user=user).exists()
    
    def get_user_role(self, user):
        """Get user's role in this store"""
        membership = self.members.filter(user=user).first()
        return membership.role if membership else None

class StoreMember(models.Model):
    ROLE_CHOICES = [
        ('owner', 'Owner'),
        ('staff', 'Staff'),
        ('viewer', 'Viewer'),  # Future: read-only access
    ]
    
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    store = models.ForeignKey(Store, on_delete=models.CASCADE, related_name='members')
    user = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.CASCADE, related_name='store_memberships')
    
    role = models.CharField(max_length=20, choices=ROLE_CHOICES)
    permissions = models.JSONField(default=dict, blank=True)  # Future: granular permissions
    
    invited_by = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='invited_members'
    )
    
    joined_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        db_table = 'store_members'
        unique_together = [['store', 'user']]
        ordering = ['-joined_at']
    
    def __str__(self):
        return f"{self.user.email} - {self.store.name} ({self.role})"
```

**Store Serializers:**

```python
# apps/stores/serializers.py
from rest_framework import serializers
from .models import Store, StoreMember
from apps.accounts.serializers import UserSerializer

class StoreSerializer(serializers.ModelSerializer):
    owner = UserSerializer(read_only=True)
    member_count = serializers.SerializerMethodField()
    user_role = serializers.SerializerMethodField()
    
    class Meta:
        model = Store
        fields = [
            'id', 'name', 'slug', 'description', 'industry',
            'currency', 'timezone', 'is_active', 'owner',
            'member_count', 'user_role', 'created_at', 'updated_at'
        ]
        read_only_fields = ['id', 'slug', 'owner', 'created_at', 'updated_at']
    
    def get_member_count(self, obj):
        return obj.members.count()
    
    def get_user_role(self, obj):
        request = self.context.get('request')
        if request and request.user.is_authenticated:
            return obj.get_user_role(request.user)
        return None
    
    def create(self, validated_data):
        request = self.context['request']
        store = Store.objects.create(owner=request.user, **validated_data)
        
        # Automatically add owner as member
        StoreMember.objects.create(
            store=store,
            user=request.user,
            role='owner'
        )
        
        return store

class StoreMemberSerializer(serializers.ModelSerializer):
    user = UserSerializer(read_only=True)
    user_email = serializers.EmailField(write_only=True, required=False)
    
    class Meta:
        model = StoreMember
        fields = ['id', 'user', 'user_email', 'role', 'joined_at']
        read_only_fields = ['id', 'joined_at']
    
    def validate_role(self, value):
        # Prevent changing owner role
        if self.instance and self.instance.role == 'owner':
            if value != 'owner':
                raise serializers.ValidationError("Cannot change owner role")
        return value
```

**Store Views:**

```python
# apps/stores/views.py
from rest_framework import viewsets, status
from rest_framework.decorators import action
from rest_framework.response import Response
from rest_framework.permissions import IsAuthenticated
from .models import Store, StoreMember
from .serializers import StoreSerializer, StoreMemberSerializer
from core.permissions import IsStoreOwner, IsStoreMember

class StoreViewSet(viewsets.ModelViewSet):
    serializer_class = StoreSerializer
    permission_classes = [IsAuthenticated]
    
    def get_queryset(self):
        # Only return stores where user is a member
        return Store.objects.filter(
            members__user=self.request.user
        ).distinct()
    
    def get_permissions(self):
        if self.action in ['update', 'partial_update', 'destroy']:
            return [IsStoreOwner()]
        return super().get_permissions()
    
    @action(detail=True, methods=['get'])
    def members(self, request, pk=None):
        store = self.get_object()
        members = store.members.all()
        serializer = StoreMemberSerializer(members, many=True)
        return Response({'data': serializer.data})
    
    @action(detail=True, methods=['post'], permission_classes=[IsStoreOwner])
    def add_member(self, request, pk=None):
        store = self.get_object()
        serializer = StoreMemberSerializer(data=request.data)
        
        if serializer.is_valid():
            # Get user by email
            from django.contrib.auth import get_user_model
            User = get_user_model()
            
            user_email = serializer.validated_data.get('user_email')
            user = User.objects.filter(email=user_email).first()
            
            if not user:
                return Response(
                    {'errors': [{'message': 'User not found'}]},
                    status=status.HTTP_404_NOT_FOUND
                )
            
            # Check if already member
            if store.has_member(user):
                return Response(
                    {'errors': [{'message': 'User is already a member'}]},
                    status=status.HTTP_400_BAD_REQUEST
                )
            
            # Create membership
            member = StoreMember.objects.create(
                store=store,
                user=user,
                role=serializer.validated_data['role'],
                invited_by=request.user
            )
            
            return Response(
                {'data': StoreMemberSerializer(member).data},
                status=status.HTTP_201_CREATED
            )
        
        return Response(
            {'errors': serializer.errors},
            status=status.HTTP_400_BAD_REQUEST
        )
```

#### 4. Permission Classes

**File:** `core/permissions.py`

```python
from rest_framework import permissions

class IsStoreOwner(permissions.BasePermission):
    """
    Permission check: User must be the store owner
    """
    def has_object_permission(self, request, view, obj):
        # Get the store object
        if hasattr(obj, 'store'):
            store = obj.store
        else:
            store = obj
        
        # Check if user is owner
        return store.members.filter(
            user=request.user,
            role='owner'
        ).exists()

class IsStoreMember(permissions.BasePermission):
    """
    Permission check: User must be a member of the store
    """
    def has_object_permission(self, request, view, obj):
        # Get the store object
        if hasattr(obj, 'store'):
            store = obj.store
        else:
            store = obj
        
        # Check membership
        return store.has_member(request.user)

class CanManageStoreMembers(permissions.BasePermission):
    """
    Permission check: Only owners can manage members
    """
    def has_permission(self, request, view):
        if not hasattr(request, 'store'):
            return False
        
        return request.store.members.filter(
            user=request.user,
            role='owner'
        ).exists()
```

#### 5. Store Context Middleware

**File:** `core/middleware.py`

```python
from apps.stores.models import Store, StoreMember

class StoreContextMiddleware:
    """
    Middleware to inject store context into requests
    This allows easy access to current store and user's role
    """
    def __init__(self, get_response):
        self.get_response = get_response
    
    def __call__(self, request):
        # Only process for authenticated users
        if request.user.is_authenticated:
            # Get store_id from header or URL parameter
            store_id = request.headers.get('X-Store-ID') or request.GET.get('store_id')
            
            if store_id:
                try:
                    # Verify membership and attach store to request
                    membership = StoreMember.objects.select_related('store').get(
                        store_id=store_id,
                        user=request.user
                    )
                    
                    request.store = membership.store
                    request.store_role = membership.role
                    request.store_membership = membership
                    
                except StoreMember.DoesNotExist:
                    # User not member of this store
                    request.store = None
                    request.store_role = None
        
        response = self.get_response(request)
        return response
```

### Testing Requirements

**Unit Tests:**

```python
# apps/accounts/tests/test_authentication.py
from django.test import TestCase
from django.contrib.auth import get_user_model
from rest_framework.test import APIClient
from rest_framework import status

User = get_user_model()

class AuthenticationTests(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.register_url = '/api/v1/auth/register/'
        self.login_url = '/api/v1/auth/login/'
    
    def test_user_registration(self):
        """Test user can register successfully"""
        data = {
            'email': 'test@example.com',
            'full_name': 'Test User',
            'password': 'TestPass123!',
            'password_confirm': 'TestPass123!'
        }
        
        response = self.client.post(self.register_url, data)
        
        self.assertEqual(response.status_code, status.HTTP_201_CREATED)
        self.assertTrue(User.objects.filter(email='test@example.com').exists())
    
    def test_user_login(self):
        """Test user can login and receive tokens"""
        # Create user
        user = User.objects.create_user(
            email='test@example.com',
            password='TestPass123!'
        )
        
        # Login
        data = {'email': 'test@example.com', 'password': 'TestPass123!'}
        response = self.client.post(self.login_url, data)
        
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.assertIn('access', response.data)
        self.assertIn('refresh', response.data)

# apps/stores/tests/test_stores.py
class StoreTests(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.user = User.objects.create_user(
            email='owner@example.com',
            password='TestPass123!'
        )
        self.client.force_authenticate(user=self.user)
    
    def test_create_store(self):
        """Test authenticated user can create a store"""
        data = {
            'name': 'Test Store',
            'description': 'A test store',
            'industry': 'Fashion'
        }
        
        response = self.client.post('/api/v1/stores/', data)
        
        self.assertEqual(response.status.HTTP_201_CREATED)
        self.assertTrue(Store.objects.filter(name='Test Store').exists())
        
        # Verify owner membership created
        store = Store.objects.get(name='Test Store')
        self.assertTrue(store.has_member(self.user))
        self.assertEqual(store.get_user_role(self.user), 'owner')
    
    def test_store_isolation(self):
        """Test users can only see their stores"""
        # Create store for user1
        store1 = Store.objects.create(owner=self.user, name='Store 1')
        StoreMember.objects.create(store=store1, user=self.user, role='owner')
        
        # Create store for user2
        user2 = User.objects.create_user(email='user2@example.com', password='pass')
        store2 = Store.objects.create(owner=user2, name='Store 2')
        StoreMember.objects.create(store=store2, user=user2, role='owner')
        
        # user1 should only see store1
        response = self.client.get('/api/v1/stores/')
        self.assertEqual(len(response.data['data']), 1)
        self.assertEqual(response.data['data'][0]['name'], 'Store 1')
```

### API Documentation Setup

**Configure drf-spectacular:**

```python
# config/settings/base.py
SPECTACULAR_SETTINGS = {
    'TITLE': 'SellPilot API',
    'DESCRIPTION': 'Multi-tenant ecommerce SaaS platform API',
    'VERSION': '1.0.0',
    'SERVE_INCLUDE_SCHEMA': False,
    'COMPONENT_SPLIT_REQUEST': True,
}

# config/urls.py
from drf_spectacular.views import SpectacularAPIView, SpectacularSwaggerView
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    
    # API v1
    path('api/v1/auth/', include('apps.accounts.urls')),
    path('api/v1/stores/', include('apps.stores.urls')),
    
    # API Documentation
    path('api/schema/', SpectacularAPIView.as_view(), name='schema'),
    path('api/docs/', SpectacularSwaggerView.as_view(url_name='schema'), name='swagger-ui'),
]
```

### Database Migrations

```bash
# Create initial migrations
python manage.py makemigrations accounts
python manage.py makemigrations stores

# Apply migrations
python manage.py migrate

# Create superuser for admin access
python manage.py createsuperuser
```

### Docker Setup for Local Development

```yaml
# docker-compose.yml
version: '3.9'

services:
  db:
    image: postgres:14-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: sellpilot_dev
      POSTGRES_USER: sellpilot
      POSTGRES_PASSWORD: sellpilot_dev_pass
    ports:
      - "5432:5432"
  
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  
  backend:
    build: ./backend
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    environment:
      - DJANGO_ENV=development
      - DATABASE_URL=postgresql://sellpilot:sellpilot_dev_pass@db:5432/sellpilot_dev
    depends_on:
      - db
      - redis

volumes:
  postgres_data:
```

### Acceptance Criteria

Phase 1 is complete when:

- [ ] Django project initialized with proper structure
- [ ] Custom User model implemented and working
- [ ] User registration endpoint functional
- [ ] JWT authentication working (login, refresh)
- [ ] Store model implemented with multi-tenancy
- [ ] Store CRUD operations working
- [ ] StoreMember model and RBAC implemented
- [ ] Permission classes enforcing access control
- [ ] Store context middleware functional
- [ ] API documentation accessible at `/api/docs/`
- [ ] All unit tests passing (>80% coverage)
- [ ] Database migrations created and applied
- [ ] Docker Compose setup working locally
- [ ] Code reviewed and merged to `develop` branch

### Success Metrics

- Registration flow takes < 500ms
- Login flow returns tokens < 300ms
- Store queries properly scoped to user membership
- No cross-store data leakage in tests
- API documentation auto-generated and accurate

---

## Phase 2: Ecommerce Core

**Status:** 📋 Planned  
**Duration:** 3-4 weeks  
**Complexity:** High  
**Dependencies:** Phase 1 complete

### Objective

Build the complete ecommerce backend system including products, variants, inventory, customers, orders, and invoice generation. This phase transforms SellPilot from an authentication system into a functional ecommerce platform.

### Why This Phase Matters

The ecommerce core is what differentiates SellPilot from a generic SaaS platform. This phase demonstrates:
- Complex data modeling (products with variants)
- Business logic implementation (order totals, inventory)
- State machine design (order workflow)
- Background task processing (invoice generation)
- Audit trail implementation

### Technical Scope

#### 1. Products Module

**Data Model:**

```python
# apps/products/models.py
import uuid
from django.db import models
from apps.stores.models import Store

class Category(models.Model):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    store = models.ForeignKey(Store, on_delete=models.CASCADE, related_name='categories')
    
    name = models.CharField(max_length=200)
    slug = models.SlugField(max_length=200)
    description = models.TextField(blank=True)
    parent = models.ForeignKey('self', null=True, blank=True, on_delete=models.CASCADE, related_name='children')
    
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        db_table = 'categories'
        unique_together = [['store', 'slug']]
        verbose_name_plural = 'Categories'
    
    def __str__(self):
        return self.name

class Product(models.Model):
    STATUS_CHOICES = [
        ('draft', 'Draft'),
        ('active', 'Active'),
        ('archived', 'Archived'),
    ]
    
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    store = models.ForeignKey(Store, on_delete=models.CASCADE, related_name='products')
    category = models.ForeignKey(Category, null=True, blank=True, on_delete=models.SET_NULL, related_name='products')
    
    title = models.CharField(max_length=300)
    description = models.TextField(blank=True)
    sku = models.CharField(max_length=100, blank=True)
    
    base_price = models.DecimalField(max_digits=10, decimal_places=2)
    compare_at_price = models.DecimalField(max_digits=10, decimal_places=2, null=True, blank=True)
    cost_per_item = models.DecimalField(max_digits=10, decimal_places=2, null=True, blank=True)
    
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='draft')
    
    # SEO fields
    meta_title = models.CharField(max_length=200, blank=True)
    meta_description = models.TextField(blank=True)
    
    # Additional data
    weight = models.DecimalField(max_digits=10, decimal_places=2, null=True, blank=True, help_text='Weight in kg')
    metadata = models.JSONField(default=dict, blank=True)
    
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        db_table = 'products'
        ordering = ['-created_at']
        indexes = [
            models.Index(fields=['store', 'status']),
            models.Index(fields=['store', 'created_at']),
        ]
    
    def __str__(self):
        return self.title
    
    @property
    def total_stock(self):
        """Calculate total stock across all variants"""
        return self.variants.aggregate(
            total=models.Sum('stock_quantity')
        )['total'] or 0

class ProductVariant(models.Model):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    product = models.ForeignKey(Product, on_delete=models.CASCADE, related_name='variants')
    
    variant_name = models.CharField(max_length=100)  # e.g., "Large / Blue"
    sku = models.CharField(max_length=100, unique=True, blank=True)
    
    price_adjustment = models.DecimalField(
        max_digits=10,
        decimal_places=2,
        default=0.00,
        help_text='Amount to add/subtract from base price'
    )
    
    stock_quantity = models.IntegerField(default=0)
    low_stock_threshold = models.IntegerField(default=10)
    
    # Variant attributes stored as JSON for flexibility
    # Example: {"size": "L", "color": "Blue", "material": "Cotton"}
    attributes = models.JSONField(default=dict)
    
    is_active = models.BooleanField(default=True)
    
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        db_table = 'product_variants'
        ordering = ['variant_name']
    
    def __str__(self):
        return f"{self.product.title} - {self.variant_name}"
    
    @property
    def final_price(self):
        """Calculate actual selling price"""
        return self.product.base_price + self.price_adjustment
    
    @property
    def is_low_stock(self):
        """Check if stock is below threshold"""
        return self.stock_quantity <= self.low_stock_threshold

class ProductImage(models.Model):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    product = models.ForeignKey(Product, on_delete=models.CASCADE, related_name='images')
    
    image_url = models.URLField(max_length=500)
    alt_text = models.CharField(max_length=200, blank=True)
    position = models.IntegerField(default=0)
    is_primary = models.BooleanField(default=False)
    
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        db_table = 'product_images'
        ordering = ['position']
    
    def __str__(self):
        return f"Image for {self.product.title}"
    
    def save(self, *args, **kwargs):
        # If this is set as primary, unset others
        if self.is_primary:
            ProductImage.objects.filter(
                product=self.product,
                is_primary=True
            ).update(is_primary=False)
        
        super().save(*args, **kwargs)
```

**Product Serializers:**

```python
# apps/products/serializers.py
from rest_framework import serializers
from .models import Product, ProductVariant, ProductImage, Category

class ProductImageSerializer(serializers.ModelSerializer):
    class Meta:
        model = ProductImage
        fields = ['id', 'image_url', 'alt_text', 'position', 'is_primary']

class ProductVariantSerializer(serializers.ModelSerializer):
    final_price = serializers.DecimalField(max_digits=10, decimal_places=2, read_only=True)
    is_low_stock = serializers.BooleanField(read_only=True)
    
    class Meta:
        model = ProductVariant
        fields = [
            'id', 'variant_name', 'sku', 'price_adjustment',
            'final_price', 'stock_quantity', 'low_stock_threshold',
            'attributes', 'is_active', 'is_low_stock'
        ]

class ProductListSerializer(serializers.ModelSerializer):
    """Lightweight serializer for product lists"""
    total_stock = serializers.IntegerField(read_only=True)
    variant_count = serializers.SerializerMethodField()
    primary_image = serializers.SerializerMethodField()
    
    class Meta:
        model = Product
        fields = [
            'id', 'title', 'sku', 'base_price', 'status',
            'total_stock', 'variant_count', 'primary_image',
            'created_at', 'updated_at'
        ]
    
    def get_variant_count(self, obj):
        return obj.variants.count()
    
    def get_primary_image(self, obj):
        primary = obj.images.filter(is_primary=True).first()
        if primary:
            return primary.image_url
        return None

class ProductDetailSerializer(serializers.ModelSerializer):
    """Full product details with variants and images"""
    variants = ProductVariantSerializer(many=True, read_only=True)
    images = ProductImageSerializer(many=True, read_only=True)
    total_stock = serializers.IntegerField(read_only=True)
    
    class Meta:
        model = Product
        fields = [
            'id', 'store', 'category', 'title', 'description',
            'sku', 'base_price', 'compare_at_price', 'cost_per_item',
            'status', 'meta_title', 'meta_description', 'weight',
            'metadata', 'total_stock', 'variants', 'images',
            'created_at', 'updated_at'
        ]
        read_only_fields = ['id', 'store', 'created_at', 'updated_at']
    
    def validate(self, attrs):
        # Ensure product belongs to request store
        request = self.context['request']
        if hasattr(request, 'store'):
            attrs['store'] = request.store
        return attrs
```

**Product Views:**

```python
# apps/products/views.py
from rest_framework import viewsets, status, filters
from rest_framework.decorators import action
from rest_framework.response import Response
from django_filters.rest_framework import DjangoFilterBackend
from .models import Product, ProductVariant
from .serializers import ProductListSerializer, ProductDetailSerializer, ProductVariantSerializer
from core.permissions import IsStoreMember

class ProductViewSet(viewsets.ModelViewSet):
    permission_classes = [IsStoreMember]
    filter_backends = [DjangoFilterBackend, filters.SearchFilter, filters.OrderingFilter]
    filterset_fields = ['status', 'category']
    search_fields = ['title', 'description', 'sku']
    ordering_fields = ['created_at', 'title', 'base_price']
    ordering = ['-created_at']
    
    def get_queryset(self):
        if not hasattr(self.request, 'store'):
            return Product.objects.none()
        
        return Product.objects.filter(
            store=self.request.store
        ).select_related('category').prefetch_related('variants', 'images')
    
    def get_serializer_class(self):
        if self.action == 'list':
            return ProductListSerializer
        return ProductDetailSerializer
    
    @action(detail=True, methods=['post'])
    def add_variant(self, request, pk=None):
        """Add a variant to a product"""
        product = self.get_object()
        serializer = ProductVariantSerializer(data=request.data)
        
        if serializer.is_valid():
            serializer.save(product=product)
            return Response(
                {'data': serializer.data},
                status=status.HTTP_201_CREATED
            )
        
        return Response(
            {'errors': serializer.errors},
            status=status.HTTP_400_BAD_REQUEST
        )
    
    @action(detail=True, methods=['post'])
    def bulk_update_stock(self, request, pk=None):
        """Update stock for multiple variants at once"""
        product = self.get_object()
        updates = request.data.get('updates', [])
        
        for update in updates:
            variant_id = update.get('variant_id')
            new_stock = update.get('stock_quantity')
            
            try:
                variant = product.variants.get(id=variant_id)
                variant.stock_quantity = new_stock
                variant.save()
            except ProductVariant.DoesNotExist:
                continue
        
        # Return updated product
        serializer = self.get_serializer(product)
        return Response({'data': serializer.data})
```

#### 2. Customers Module

**Customer Model:**

```python
# apps/customers/models.py
import uuid
from django.db import models
from apps.stores.models import Store

class Customer(models.Model):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    store = models.ForeignKey(Store, on_delete=models.CASCADE, related_name='customers')
    
    email = models.EmailField()
    first_name = models.CharField(max_length=100)
    last_name = models.CharField(max_length=100)
    phone = models.CharField(max_length=20, blank=True)
    
    # Address information
    address_line1 = models.CharField(max_length=200, blank=True)
    address_line2 = models.CharField(max_length=200, blank=True)
    city = models.CharField(max_length=100, blank=True)
    state = models.CharField(max_length=100, blank=True)
    postal_code = models.CharField(max_length=20, blank=True)
    country = models.CharField(max_length=2, blank=True)
    
    # Customer metadata
    tags = models.JSONField(default=list, blank=True)  # e.g., ["VIP", "Wholesale"]
    notes = models.TextField(blank=True)
    marketing_consent = models.BooleanField(default=False)
    
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        db_table = 'customers'
        unique_together = [['store', 'email']]
        ordering = ['-created_at']
        indexes = [
            models.Index(fields=['store', 'email']),
            models.Index(fields=['store', 'created_at']),
        ]
    
    def __str__(self):
        return f"{self.first_name} {self.last_name} ({self.email})"
    
    @property
    def full_name(self):
        return f"{self.first_name} {self.last_name}"
    
    @property
    def order_count(self):
        """Count of orders placed by this customer"""
        return self.orders.count()
    
    @property
    def lifetime_value(self):
        """Total amount spent by customer"""
        from django.db.models import Sum
        total = self.orders.filter(
            status__in=['processing', 'shipped', 'delivered']
        ).aggregate(total=Sum('total_amount'))['total']
        return total or 0
```

#### 3. Orders Module

**Order Models:**

```python
# apps/orders/models.py
import uuid
from decimal import Decimal
from django.db import models
from django.utils import timezone
from apps.stores.models import Store
from apps.customers.models import Customer
from apps.products.models import Product, ProductVariant

class Order(models.Model):
    STATUS_CHOICES = [
        ('pending', 'Pending'),
        ('processing', 'Processing'),
        ('shipped', 'Shipped'),
        ('delivered', 'Delivered'),
        ('cancelled', 'Cancelled'),
        ('refunded', 'Refunded'),
    ]
    
    # Valid state transitions
    VALID_TRANSITIONS = {
        'pending': ['processing', 'cancelled'],
        'processing': ['shipped', 'cancelled'],
        'shipped': ['delivered', 'cancelled'],
        'delivered': ['refunded'],
        'cancelled': [],
        'refunded': [],
    }
    
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    store = models.ForeignKey(Store, on_delete=models.CASCADE, related_name='orders')
    customer = models.ForeignKey(Customer, on_delete=models.PROTECT, related_name='orders')
    
    order_number = models.CharField(max_length=50, unique=True)
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='pending')
    
    # Financial details
    subtotal = models.DecimalField(max_digits=10, decimal_places=2)
    tax_amount = models.DecimalField(max_digits=10, decimal_places=2, default=0)
    shipping_cost = models.DecimalField(max_digits=10, decimal_places=2, default=0)
    discount_amount = models.DecimalField(max_digits=10, decimal_places=2, default=0)
    total_amount = models.DecimalField(max_digits=10, decimal_places=2)
    
    # Shipping information
    shipping_address = models.JSONField(default=dict)
    tracking_number = models.CharField(max_length=100, blank=True)
    carrier = models.CharField(max_length=50, blank=True)
    
    # Payment information
    payment_method = models.CharField(max_length=50, blank=True)
    payment_status = models.CharField(max_length=20, default='unpaid')
    paid_at = models.DateTimeField(null=True, blank=True)
    
    # Metadata
    notes = models.TextField(blank=True)
    metadata = models.JSONField(default=dict, blank=True)
    
    # Timestamps
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    shipped_at = models.DateTimeField(null=True, blank=True)
    delivered_at = models.DateTimeField(null=True, blank=True)
    
    class Meta:
        db_table = 'orders'
        ordering = ['-created_at']
        indexes = [
            models.Index(fields=['store', 'status']),
            models.Index(fields=['store', 'created_at']),
            models.Index(fields=['customer', '-created_at']),
        ]
    
    def __str__(self):
        return self.order_number
    
    def save(self, *args, **kwargs):
        if not self.order_number:
            self.order_number = self.generate_order_number()
        super().save(*args, **kwargs)
    
    def generate_order_number(self):
        """Generate unique order number"""
        import random
        import string
        prefix = self.store.slug.upper()[:3]
        suffix = ''.join(random.choices(string.digits, k=6))
        return f"{prefix}-{suffix}"
    
    def calculate_totals(self):
        """Calculate order totals from line items"""
        self.subtotal = sum(item.line_total for item in self.items.all())
        self.total_amount = (
            self.subtotal +
            self.tax_amount +
            self.shipping_cost -
            self.discount_amount
        )
        self.save()
    
    def can_transition_to(self, new_status):
        """Check if status transition is valid"""
        return new_status in self.VALID_TRANSITIONS.get(self.status, [])
    
    def transition_to(self, new_status, user=None):
        """Transition to new status with validation"""
        if not self.can_transition_to(new_status):
            raise ValueError(
                f"Cannot transition from {self.status} to {new_status}"
            )
        
        old_status = self.status
        self.status = new_status
        
        # Update timestamps
        if new_status == 'shipped':
            self.shipped_at = timezone.now()
        elif new_status == 'delivered':
            self.delivered_at = timezone.now()
        
        self.save()
        
        # Create audit log
        OrderStatusLog.objects.create(
            order=self,
            old_status=old_status,
            new_status=new_status,
            changed_by=user
        )
        
        return True

class OrderItem(models.Model):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    order = models.ForeignKey(Order, on_delete=models.CASCADE, related_name='items')
    product = models.ForeignKey(Product, on_delete=models.PROTECT)
    variant = models.ForeignKey(ProductVariant, null=True, blank=True, on_delete=models.PROTECT)
    
    quantity = models.IntegerField(default=1)
    unit_price = models.DecimalField(max_digits=10, decimal_places=2)
    line_total = models.DecimalField(max_digits=10, decimal_places=2)
    
    # Snapshot of product details at time of order
    product_title = models.CharField(max_length=300)
    variant_name = models.CharField(max_length=100, blank=True)
    
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        db_table = 'order_items'
    
    def save(self, *args, **kwargs):
        # Calculate line total
        self.line_total = self.unit_price * self.quantity
        
        # Snapshot product details
        self.product_title = self.product.title
        if self.variant:
            self.variant_name = self.variant.variant_name
        
        super().save(*args, **kwargs)
    
    def __str__(self):
        return f"{self.product_title} x {self.quantity}"

class OrderStatusLog(models.Model):
    """Audit trail for order status changes"""
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    order = models.ForeignKey(Order, on_delete=models.CASCADE, related_name='status_logs')
    
    old_status = models.CharField(max_length=20)
    new_status = models.CharField(max_length=20)
    
    changed_by = models.ForeignKey(
        'accounts.User',
        null=True,
        blank=True,
        on_delete=models.SET_NULL
    )
    
    notes = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        db_table = 'order_status_logs'
        ordering = ['-created_at']
    
    def __str__(self):
        return f"{self.order.order_number}: {self.old_status} → {self.new_status}"
```

**Order Serializers:**

```python
# apps/orders/serializers.py
from rest_framework import serializers
from .models import Order, OrderItem
from apps.customers.serializers import CustomerSerializer
from apps.products.models import ProductVariant

class OrderItemSerializer(serializers.ModelSerializer):
    class Meta:
        model = OrderItem
        fields = [
            'id', 'product', 'variant', 'product_title',
            'variant_name', 'quantity', 'unit_price', 'line_total'
        ]
        read_only_fields = ['line_total', 'product_title', 'variant_name']

class OrderCreateSerializer(serializers.ModelSerializer):
    items = OrderItemSerializer(many=True)
    
    class Meta:
        model = Order
        fields = [
            'customer', 'items', 'shipping_address',
            'shipping_cost', 'tax_amount', 'discount_amount', 'notes'
        ]
    
    def create(self, validated_data):
        items_data = validated_data.pop('items')
        request = self.context['request']
        
        # Create order
        order = Order.objects.create(
            store=request.store,
            subtotal=0,  # Will be calculated
            total_amount=0,  # Will be calculated
            **validated_data
        )
        
        # Create order items and reserve inventory
        for item_data in items_data:
            variant = item_data.get('variant')
            quantity = item_data['quantity']
            
            # Check and reserve stock
            if variant:
                if variant.stock_quantity < quantity:
                    raise serializers.ValidationError(
                        f"Insufficient stock for {variant.variant_name}"
                    )
                variant.stock_quantity -= quantity
                variant.save()
            
            # Create order item
            OrderItem.objects.create(order=order, **item_data)
        
        # Calculate totals
        order.calculate_totals()
        
        return order

class OrderListSerializer(serializers.ModelSerializer):
    customer_name = serializers.CharField(source='customer.full_name', read_only=True)
    item_count = serializers.SerializerMethodField()
    
    class Meta:
        model = Order
        fields = [
            'id', 'order_number', 'customer_name', 'status',
            'total_amount', 'item_count', 'created_at'
        ]
    
    def get_item_count(self, obj):
        return obj.items.count()

class OrderDetailSerializer(serializers.ModelSerializer):
    customer = CustomerSerializer(read_only=True)
    items = OrderItemSerializer(many=True, read_only=True)
    can_cancel = serializers.SerializerMethodField()
    
    class Meta:
        model = Order
        fields = [
            'id', 'order_number', 'customer', 'status', 'items',
            'subtotal', 'tax_amount', 'shipping_cost', 'discount_amount',
            'total_amount', 'shipping_address', 'tracking_number',
            'carrier', 'payment_method', 'payment_status', 'notes',
            'can_cancel', 'created_at', 'updated_at', 'shipped_at', 'delivered_at'
        ]
    
    def get_can_cancel(self, obj):
        return obj.can_transition_to('cancelled')
```

#### 4. Invoices Module

**Invoice Model:**

```python
# apps/invoices/models.py
import uuid
from django.db import models
from apps.stores.models import Store
from apps.orders.models import Order

class Invoice(models.Model):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    store = models.ForeignKey(Store, on_delete=models.CASCADE, related_name='invoices')
    order = models.OneToOneField(Order, on_delete=models.CASCADE, related_name='invoice')
    
    invoice_number = models.CharField(max_length=50, unique=True)
    pdf_url = models.URLField(max_length=500, blank=True)
    
    issued_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        db_table = 'invoices'
        ordering = ['-issued_at']
    
    def __str__(self):
        return self.invoice_number
    
    def generate_invoice_number(self):
        """Generate unique invoice number"""
        import random
        import string
        year = timezone.now().year
        suffix = ''.join(random.choices(string.digits, k=6))
        return f"INV-{year}-{suffix}"
```

**Celery Task for PDF Generation:**

```python
# apps/invoices/tasks.py
from celery import shared_task
from django.template.loader import render_to_string
from weasyprint import HTML
import boto3
from django.conf import settings
from .models import Invoice

@shared_task
def generate_invoice_pdf(invoice_id):
    """
    Generate PDF invoice and upload to S3
    This runs asynchronously to avoid blocking API requests
    """
    try:
        invoice = Invoice.objects.select_related(
            'order', 'order__customer', 'store'
        ).prefetch_related('order__items').get(id=invoice_id)
        
        # Render HTML template
        html_content = render_to_string('invoices/invoice_template.html', {
            'invoice': invoice,
            'order': invoice.order,
            'store': invoice.store,
            'customer': invoice.order.customer,
            'items': invoice.order.items.all(),
        })
        
        # Generate PDF
        pdf = HTML(string=html_content).write_pdf()
        
        # Upload to S3
        s3_client = boto3.client('s3')
        file_key = f"invoices/{invoice.store.id}/{invoice.invoice_number}.pdf"
        
        s3_client.put_object(
            Bucket=settings.AWS_STORAGE_BUCKET_NAME,
            Key=file_key,
            Body=pdf,
            ContentType='application/pdf'
        )
        
        # Update invoice with S3 URL
        invoice.pdf_url = f"https://{settings.AWS_STORAGE_BUCKET_NAME}.s3.amazonaws.com/{file_key}"
        invoice.save()
        
        return {'success': True, 'pdf_url': invoice.pdf_url}
        
    except Exception as e:
        return {'success': False, 'error': str(e)}
```

#### 5. Analytics Module

**Daily Metrics Model:**

```python
# apps/analytics/models.py
import uuid
from django.db import models
from apps.stores.models import Store

class DailyMetrics(models.Model):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    store = models.ForeignKey(Store, on_delete=models.CASCADE, related_name='daily_metrics')
    
    date = models.DateField()
    
    # Sales metrics
    revenue = models.DecimalField(max_digits=12, decimal_places=2, default=0)
    orders_count = models.IntegerField(default=0)
    units_sold = models.IntegerField(default=0)
    avg_order_value = models.DecimalField(max_digits=10, decimal_places=2, default=0)
    
    # Customer metrics
    new_customers = models.IntegerField(default=0)
    returning_customers = models.IntegerField(default=0)
    
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        db_table = 'daily_metrics'
        unique_together = [['store', 'date']]
        ordering = ['-date']
        indexes = [
            models.Index(fields=['store', '-date']),
        ]
    
    def __str__(self):
        return f"{self.store.name} - {self.date}"
```

**Celery Task for Daily Aggregation:**

```python
# apps/analytics/tasks.py
from celery import shared_task
from django.utils import timezone
from datetime import timedelta
from decimal import Decimal
from django.db.models import Sum, Count, Avg
from apps.stores.models import Store
from apps.orders.models import Order
from apps.customers.models import Customer
from .models import DailyMetrics

@shared_task
def aggregate_daily_metrics():
    """
    Run nightly to aggregate yesterday's metrics
    Should be scheduled via Celery Beat
    """
    yesterday = (timezone.now() - timedelta(days=1)).date()
    
    for store in Store.objects.filter(is_active=True):
        # Get yesterday's orders
        orders = Order.objects.filter(
            store=store,
            created_at__date=yesterday,
            status__in=['processing', 'shipped', 'delivered']
        )
        
        # Calculate metrics
        metrics = orders.aggregate(
            revenue=Sum('total_amount'),
            orders_count=Count('id'),
            units_sold=Sum('items__quantity'),
            avg_order_value=Avg('total_amount')
        )
        
        # Count new vs returning customers
        customer_ids = orders.values_list('customer_id', flat=True)
        new_customers = Customer.objects.filter(
            id__in=customer_ids,
            created_at__date=yesterday
        ).count()
        
        returning_customers = len(customer_ids) - new_customers
        
        # Create or update daily metrics
        DailyMetrics.objects.update_or_create(
            store=store,
            date=yesterday,
            defaults={
                'revenue': metrics['revenue'] or Decimal('0'),
                'orders_count': metrics['orders_count'] or 0,
                'units_sold': metrics['units_sold'] or 0,
                'avg_order_value': metrics['avg_order_value'] or Decimal('0'),
                'new_customers': new_customers,
                'returning_customers': returning_customers,
            }
        )
    
    return f"Aggregated metrics for {yesterday}"
```

### Celery Configuration

**File:** `config/celery.py`

```python
import os
from celery import Celery
from celery.schedules import crontab

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings')

app = Celery('sellpilot')
app.config_from_object('django.conf:settings', namespace='CELERY')
app.autodiscover_tasks()

# Celery Beat schedule
app.conf.beat_schedule = {
    'aggregate-daily-metrics': {
        'task': 'apps.analytics.tasks.aggregate_daily_metrics',
        'schedule': crontab(hour=1, minute=0),  # Run daily at 1 AM
    },
}
```

### Testing Requirements

**Key Test Scenarios:**

```python
# apps/products/tests/test_products.py
class ProductTests(TestCase):
    def test_create_product_with_variants(self):
        """Test creating a product with multiple variants"""
        # Test implementation
    
    def test_stock_reservation_on_order(self):
        """Test inventory decrements when order placed"""
        # Test implementation
    
    def test_low_stock_alert(self):
        """Test low stock threshold detection"""
        # Test implementation

# apps/orders/tests/test_orders.py
class OrderTests(TestCase):
    def test_order_creation_and_totals(self):
        """Test order total calculation"""
        # Test implementation
    
    def test_order_status_transitions(self):
        """Test valid and invalid status transitions"""
        # Test implementation
    
    def test_inventory_rollback_on_failed_order(self):
        """Test stock returns to inventory if order fails"""
        # Test implementation
```

### Acceptance Criteria

Phase 2 is complete when:

- [ ] Products CRUD fully functional
- [ ] Product variants working with stock tracking
- [ ] Categories and product images implemented
- [ ] Customers CRUD functional
- [ ] Orders can be created with inventory reservation
- [ ] Order status workflow implemented and tested
- [ ] Invoice generation working (PDF + S3 storage)
- [ ] Analytics daily aggregation task functional
- [ ] Celery + Redis integrated and working
- [ ] All database models created and migrated
- [ ] API endpoints documented in Swagger
- [ ] Test coverage >80% for core business logic
- [ ] Store-scoping enforced on all resources
- [ ] Audit logs created for critical operations

### Success Metrics

- Order creation completes in < 1 second
- PDF generation completes in < 5 seconds (async)
- Product queries return in < 200ms with pagination
- Inventory updates are atomic (no race conditions)
- Daily metrics aggregation runs successfully

---

## Phase 3: Cloud Infrastructure

**Status:** 📋 Planned  
**Duration:** 2 weeks  
**Complexity:** Medium  
**Dependencies:** Phase 2 complete

### Objective

Transform SellPilot from a local development system into a production-ready cloud application. This phase implements AWS infrastructure integration, scalable media handling, environment-based configuration, and monitoring—demonstrating real-world DevOps capabilities.

### Why This Phase Matters

This phase bridges the gap between "works on my machine" and production deployment. Recruiters and technical evaluators specifically look for:
- Cloud deployment experience
- Understanding of managed services
- Security best practices (IAM, secrets management)
- Monitoring and observability
- Infrastructure reliability

### Technical Scope

#### 1. AWS RDS PostgreSQL Setup

**Why RDS over EC2-hosted PostgreSQL:**
- Automated backups with point-in-time recovery
- Automatic OS and database patching
- Multi-AZ failover for high availability
- Performance insights and monitoring
- Easier to scale vertically and horizontally

**Setup Steps:**

1. **Create RDS Instance**
   ```bash
   # Via AWS Console or Terraform
   - Engine: PostgreSQL 14
   - Instance class: db.t3.micro (dev), db.t3.medium (prod)
   - Storage: 20GB General Purpose SSD (auto-scaling enabled)
   - Multi-AZ: No (dev), Yes (production)
   - Public accessibility: No (access via VPC only)
   - Enable automated backups (7-day retention)
   - Enable Performance Insights
   ```

2. **Security Group Configuration**
   ```
   Inbound Rules:
   - Port 5432 from EC2 security group only
   - No public access
   
   Outbound Rules:
   - Allow all (default)
   ```

3. **Update Django Settings**
   ```python
   # config/settings/production.py
   DATABASES = {
       'default': {
           'ENGINE': 'django.db.backends.postgresql',
           'NAME': config('RDS_DB_NAME'),
           'USER': config('RDS_USERNAME'),
           'PASSWORD': config('RDS_PASSWORD'),
           'HOST': config('RDS_HOSTNAME'),
           'PORT': '5432',
           'OPTIONS': {
               'connect_timeout': 10,
               'options': '-c statement_timeout=30000'  # 30s query timeout
           },
           'CONN_MAX_AGE': 600,  # Connection pooling
       }
   }
   ```

#### 2. AWS S3 Media Storage

**Why S3:**
- Unlimited scalable storage
- 99.999999999% (11 9's) durability
- Versioning for data recovery
- Lifecycle policies for cost optimization
- Native CloudFront integration

**Implementation:**

1. **Create S3 Buckets**
   ```
   Bucket structure:
   - sellpilot-media-production/
     ├── products/
     ├── invoices/
     └── temp/
   
   Settings:
   - Block all public access: Yes
   - Versioning: Enabled
   - Encryption: AES-256 (server-side)
   - Lifecycle rules:
     - Delete temp/ objects after 7 days
     - Transition invoices/ to Glacier after 90 days
   ```

2. **IAM Policy for S3 Access**
   ```json
   {
     "Version": "2012-10-17",
     "Statement": [
       {
         "Effect": "Allow",
         "Action": [
           "s3:PutObject",
           "s3:GetObject",
           "s3:DeleteObject",
           "s3:ListBucket"
         ],
         "Resource": [
           "arn:aws:s3:::sellpilot-media-production",
           "arn:aws:s3:::sellpilot-media-production/*"
         ]
       }
     ]
   }
   ```

3. **Django Storage Configuration**
   ```python
   # Install django-storages and boto3
   pip install django-storages[boto3]==1.14
   
   # config/settings/production.py
   INSTALLED_APPS += ['storages']
   
   AWS_ACCESS_KEY_ID = config('AWS_ACCESS_KEY_ID')
   AWS_SECRET_ACCESS_KEY = config('AWS_SECRET_ACCESS_KEY')
   AWS_STORAGE_BUCKET_NAME = config('AWS_STORAGE_BUCKET_NAME')
   AWS_S3_REGION_NAME = config('AWS_REGION', default='us-east-1')
   
   # Custom storage backends
   AWS_S3_CUSTOM_DOMAIN = f'{AWS_STORAGE_BUCKET_NAME}.s3.amazonaws.com'
   AWS_DEFAULT_ACL = 'private'
   AWS_S3_OBJECT_PARAMETERS = {
       'CacheControl': 'max-age=86400',
   }
   
   # Media files
   DEFAULT_FILE_STORAGE = 'storages.backends.s3boto3.S3Boto3Storage'
   MEDIA_URL = f'https://{AWS_S3_CUSTOM_DOMAIN}/'
   
   # Static files (future: serve via CDN)
   STATICFILES_STORAGE = 'storages.backends.s3boto3.S3StaticStorage'
   STATIC_URL = f'https://{AWS_S3_CUSTOM_DOMAIN}/static/'
   ```

4. **Presigned URLs for Secure Uploads**
   ```python
   # apps/products/views.py
   import boto3
   from django.conf import settings
   
   @action(detail=False, methods=['post'])
   def get_upload_url(self, request):
       """Generate presigned URL for direct browser upload to S3"""
       filename = request.data.get('filename')
       content_type = request.data.get('content_type')
       
       s3_client = boto3.client('s3')
       file_key = f"products/{request.store.id}/{filename}"
       
       presigned_url = s3_client.generate_presigned_url(
           'put_object',
           Params={
               'Bucket': settings.AWS_STORAGE_BUCKET_NAME,
               'Key': file_key,
               'ContentType': content_type,
           },
           ExpiresIn=3600  # 1 hour
       )
       
       return Response({
           'upload_url': presigned_url,
           'file_key': file_key
       })
   ```

#### 3. AWS EC2 Backend Deployment

**EC2 Instance Setup:**

1. **Launch Configuration**
   ```
   AMI: Ubuntu 22.04 LTS
   Instance type: t3.small (1 vCPU, 2GB RAM)
   Storage: 30GB gp3 SSD
   Security group:
     - SSH (22) from your IP only
     - HTTP (80) from anywhere
     - HTTPS (443) from anywhere
   IAM role: EC2-SellPilot-Role (S3, CloudWatch access)
   ```

2. **Server Setup Script**
   ```bash
   #!/bin/bash
   # Initial server configuration
   
   # Update system
   sudo apt update && sudo apt upgrade -y
   
   # Install Docker
   curl -fsSL https://get.docker.com -o get-docker.sh
   sudo sh get-docker.sh
   sudo usermod -aG docker ubuntu
   
   # Install Docker Compose
   sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
   sudo chmod +x /usr/local/bin/docker-compose
   
   # Install CloudWatch agent
   wget https://s3.amazonaws.com/amazoncloudwatch-agent/ubuntu/amd64/latest/amazon-cloudwatch-agent.deb
   sudo dpkg -i amazon-cloudwatch-agent.deb
   
   # Clone repository
   git clone https://github.com/yourusername/sellpilot.git /home/ubuntu/sellpilot
   cd /home/ubuntu/sellpilot
   
   # Create production environment file
   cat > .env.production << EOF
   DJANGO_ENV=production
   SECRET_KEY=<generated-secret>
   DEBUG=False
   ALLOWED_HOSTS=api.sellpilot.io
   
   RDS_DB_NAME=<db-name>
   RDS_USERNAME=<username>
   RDS_PASSWORD=<password>
   RDS_HOSTNAME=<rds-endpoint>
   
   AWS_ACCESS_KEY_ID=<key>
   AWS_SECRET_ACCESS_KEY=<secret>
   AWS_STORAGE_BUCKET_NAME=sellpilot-media-production
   AWS_REGION=us-east-1
   
   REDIS_URL=redis://localhost:6379/0
   EOF
   
   # Start services
   docker-compose -f docker-compose.prod.yml up -d
   ```

3. **Production Docker Compose**
   ```yaml
   # docker-compose.prod.yml
   version: '3.9'
   
   services:
     nginx:
       image: nginx:alpine
       ports:
         - "80:80"
         - "443:443"
       volumes:
         - ./infrastructure/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
         - ./certbot/conf:/etc/letsencrypt:ro
         - ./certbot/www:/var/www/certbot:ro
       depends_on:
         - api
     
     api:
       build:
         context: ./backend
         dockerfile: Dockerfile.prod
       command: gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 4
       env_file:
         - .env.production
       volumes:
         - static_volume:/app/staticfiles
       depends_on:
         - redis
     
     celery:
       build:
         context: ./backend
         dockerfile: Dockerfile.prod
       command: celery -A config worker -l info --concurrency=2
       env_file:
         - .env.production
       depends_on:
         - redis
     
     celery-beat:
       build:
         context: ./backend
         dockerfile: Dockerfile.prod
       command: celery -A config beat -l info
       env_file:
         - .env.production
       depends_on:
         - redis
     
     redis:
       image: redis:7-alpine
       volumes:
         - redis_data:/data
   
   volumes:
     static_volume:
     redis_data:
   ```

4. **Production Dockerfile**
   ```dockerfile
   # backend/Dockerfile.prod
   FROM python:3.11-slim
   
   ENV PYTHONUNBUFFERED=1 \
       PYTHONDONTWRITEBYTECODE=1
   
   WORKDIR /app
   
   # Install system dependencies
   RUN apt-get update && apt-get install -y \
       gcc \
       postgresql-client \
       libpq-dev \
       && rm -rf /var/lib/apt/lists/*
   
   # Install Python dependencies
   COPY requirements/production.txt requirements.txt
   RUN pip install --no-cache-dir -r requirements.txt
   
   # Copy application
   COPY . .
   
   # Collect static files
   RUN python manage.py collectstatic --noinput
   
   # Create non-root user
   RUN useradd -m -u 1000 appuser && chown -R appuser:appuser /app
   USER appuser
   
   EXPOSE 8000
   ```

#### 4. Nginx Reverse Proxy

**Configuration:**

```nginx
# infrastructure/nginx/nginx.conf
upstream api_backend {
    server api:8000;
}

server {
    listen 80;
    server_name api.sellpilot.io;
    
    # Redirect to HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.sellpilot.io;
    
    # SSL certificates (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/api.sellpilot.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.sellpilot.io/privkey.pem;
    
    # SSL optimization
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Strict-Transport-Security "max-age=31536000" always;
    
    # Client body size (for file uploads)
    client_max_body_size 10M;
    
    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;
    gzip_min_length 1000;
    
    # API proxy
    location / {
        proxy_pass http://api_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # Static files (if not using CDN)
    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

#### 5. CloudWatch Logging & Monitoring

**CloudWatch Agent Configuration:**

```json
{
  "logs": {
    "logs_collected": {
      "files": {
        "collect_list": [
          {
            "file_path": "/var/log/nginx/access.log",
            "log_group_name": "/aws/ec2/sellpilot/nginx/access",
            "log_stream_name": "{instance_id}"
          },
          {
            "file_path": "/var/log/nginx/error.log",
            "log_group_name": "/aws/ec2/sellpilot/nginx/error",
            "log_stream_name": "{instance_id}"
          }
        ]
      }
    }
  },
  "metrics": {
    "namespace": "SellPilot/Production",
    "metrics_collected": {
      "cpu": {
        "measurement": [
          {"name": "cpu_usage_idle", "unit": "Percent"},
          {"name": "cpu_usage_iowait", "unit": "Percent"}
        ],
        "metrics_collection_interval": 60
      },
      "disk": {
        "measurement": [
          {"name": "used_percent", "unit": "Percent"}
        ],
        "metrics_collection_interval": 60
      },
      "mem": {
        "measurement": [
          {"name": "mem_used_percent", "unit": "Percent"}
        ],
        "metrics_collection_interval": 60
      }
    }
  }
}
```

**Application Logging:**

```python
# config/settings/production.py
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'verbose': {
            'format': '{levelname} {asctime} {module} {process:d} {thread:d} {message}',
            'style': '{',
        },
        'json': {
            '()': 'pythonjsonlogger.jsonlogger.JsonFormatter',
            'format': '%(asctime)s %(name)s %(levelname)s %(message)s'
        }
    },
    'handlers': {
        'console': {
            'class': 'logging.StreamHandler',
            'formatter': 'json',
        },
        'cloudwatch': {
            'class': 'watchtower.CloudWatchLogHandler',
            'log_group': '/aws/sellpilot/application',
            'stream_name': 'api',
            'formatter': 'json',
        },
    },
    'root': {
        'handlers': ['console', 'cloudwatch'],
        'level': 'INFO',
    },
    'loggers': {
        'django': {
            'handlers': ['console', 'cloudwatch'],
            'level': 'INFO',
            'propagate': False,
        },
    },
}
```

#### 6. CloudFront CDN (Optional but Recommended)

**Configuration:**

```
Origin: S3 bucket (sellpilot-media-production)
Cache Behaviors:
  - /products/*: Cache for 24 hours
  - /invoices/*: Cache for 7 days
  
SSL Certificate: AWS Certificate Manager (ACM)
Custom domain: cdn.sellpilot.io

Security:
  - Restrict S3 access to CloudFront only (Origin Access Identity)
  - Enable HTTPS only
  - Add custom headers for security
```

### Environment Variable Management

**AWS Secrets Manager (Production):**

```python
# config/settings/production.py
import boto3
import json

def get_secret(secret_name):
    client = boto3.client('secretsmanager', region_name='us-east-1')
    response = client.get_secret_value(SecretId=secret_name)
    return json.loads(response['SecretString'])

# Load secrets
secrets = get_secret('sellpilot/production/secrets')

SECRET_KEY = secrets['DJANGO_SECRET_KEY']
DATABASE_PASSWORD = secrets['RDS_PASSWORD']
# ... other secrets
```

**Local .env files (Development):**

```bash
# .env.example
DJANGO_ENV=development
DEBUG=True
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

DATABASE_URL=postgresql://user:pass@localhost:5432/sellpilot_dev

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=sellpilot-media-dev
AWS_REGION=us-east-1

REDIS_URL=redis://localhost:6379/0
```

### Health Checks & Monitoring

**Django Health Check Endpoint:**

```python
# core/views.py
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import AllowAny
from rest_framework.response import Response
from django.db import connection
from django.core.cache import cache

@api_view(['GET'])
@permission_classes([AllowAny])
def health_check(request):
    """
    Health check endpoint for load balancers and monitoring
    """
    checks = {
        'database': False,
        'cache': False,
        'storage': False,
    }
    
    # Database check
    try:
        with connection.cursor() as cursor:
            cursor.execute("SELECT 1")
        checks['database'] = True
    except Exception:
        pass
    
    # Cache check
    try:
        cache.set('health_check', 'ok', 10)
        checks['cache'] = cache.get('health_check') == 'ok'
    except Exception:
        pass
    
    # S3 check (optional)
    try:
        import boto3
        s3 = boto3.client('s3')
        s3.head_bucket(Bucket=settings.AWS_STORAGE_BUCKET_NAME)
        checks['storage'] = True
    except Exception:
        pass
    
    all_healthy = all(checks.values())
    status_code = 200 if all_healthy else 503
    
    return Response({
        'status': 'healthy' if all_healthy else 'unhealthy',
        'checks': checks
    }, status=status_code)
```

### Acceptance Criteria

Phase 3 is complete when:

- [ ] RDS PostgreSQL instance running and accessible
- [ ] Django connected to RDS successfully
- [ ] S3 bucket created and configured
- [ ] Django media uploads working via S3
- [ ] Presigned URL upload flow tested
- [ ] EC2 instance provisioned and configured
- [ ] Docker containers running on EC2
- [ ] Nginx reverse proxy configured
- [ ] SSL certificate installed (Let's Encrypt)
- [ ] CloudWatch agent sending logs and metrics
- [ ] Health check endpoint responding correctly
- [ ] CloudFront CDN configured (if implemented)
- [ ] Environment variables properly managed
- [ ] Production deployment documented
- [ ] Disaster recovery plan documented

### Success Metrics

- RDS connection latency < 10ms (same region)
- S3 upload completes in < 2 seconds for 5MB file
- API response time < 300ms (p95)
- No database connection pool exhaustion
- Zero failed health checks over 24 hours
- Logs successfully streaming to CloudWatch

---

## Phase 4: Frontend Dashboard

**Status:** 📋 Planned  
**Duration:** 4-5 weeks  
**Complexity:** High  
**Dependencies:** Phase 3 complete

### Objective

Build a production-quality admin dashboard that rivals Shopify's interface in terms of usability and polish. This phase demonstrates modern frontend engineering skills, UI/UX design sensibility, and the ability to create complex, data-driven interfaces.

### Why This Phase Matters

The frontend is what users (and recruiters) actually see and interact with. A well-executed dashboard:
- Demonstrates full-stack capabilities
- Shows attention to UX details
- Proves ability to work with modern React patterns
- Highlights responsive design skills
- Showcases state management expertise

### Technical Scope

#### 1. Next.js Project Setup

**Initialize Project:**

```bash
npx create-next-app@latest frontend --typescript --tailwind --app --no-src-dir
cd frontend
```

**Install Dependencies:**

```bash
npm install
  axios \
  @tanstack/react-query \
  zustand \
  react-hook-form \
  zod \
  @hookform/resolvers \
  recharts \
  date-fns \
  @headlessui/react \
  @heroicons/react \
  clsx \
  tailwind-merge
```

**Project Structure:**

```
frontend/
├── app/
│   ├── (auth)/
│   │   ├── login/
│   │   │   └── page.tsx
│   │   ├── register/
│   │   │   └── page.tsx
│   │   └── layout.tsx
│   │
│   ├── (dashboard)/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   │
│   │   ├── products/
│   │   │   ├── page.tsx
│   │   │   ├── new/
│   │   │   │   └── page.tsx
│   │   │   └── [id]/
│   │   │       ├── page.tsx
│   │   │       └── edit/
│   │   │           └── page.tsx
│   │   │
│   │   ├── orders/
│   │   │   ├── page.tsx
│   │   │   └── [id]/
│   │   │       └── page.tsx
│   │   │
│   │   ├── customers/
│   │   │   ├── page.tsx
│   │   │   └── [id]/
│   │   │       └── page.tsx
│   │   │
│   │   ├── analytics/
│   │   │   └── page.tsx
│   │   │
│   │   └── settings/
│   │       ├── page.tsx
│   │       └── team/
│   │           └── page.tsx
│   │
│   ├── api/
│   │   └── upload/
│   │       └── route.ts
│   │
│   └── layout.tsx
│
├── components/
│   ├── ui/              # Base components
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   ├── Select.tsx
│   │   ├── Modal.tsx
│   │   ├── Table.tsx
│   │   ├── Card.tsx
│   │   └── Badge.tsx
│   │
│   ├── forms/
│   │   ├── ProductForm.tsx
│   │   ├── OrderForm.tsx
│   │   └── CustomerForm.tsx
│   │
│   ├── charts/
│   │   ├── RevenueChart.tsx
│   │   └── OrdersChart.tsx
│   │
│   └── layouts/
│       ├── DashboardLayout.tsx
│       ├── Sidebar.tsx
│       └── Header.tsx
│
├── lib/
│   ├── api/
│   │   ├── client.ts
│   │   ├── auth.ts
│   │   ├── products.ts
│   │   ├── orders.ts
│   │   └── analytics.ts
│   │
│   ├── hooks/
│   │   ├── useAuth.ts
│   │   ├── useStore.ts
│   │   └── useProducts.ts
│   │
│   ├── types/
│   │   ├── product.ts
│   │   ├── order.ts
│   │   └── customer.ts
│   │
│   └── utils/
│       ├── formatters.ts
│       ├── validators.ts
│       └── cn.ts
│
└── store/
    ├── authStore.ts
    └── storeStore.ts
```

#### 2. Authentication Implementation

**API Client Setup:**

```typescript
// lib/api/client.ts
import axios, { AxiosInstance, AxiosRequestConfig } from 'axios';

class ApiClient {
  private client: AxiosInstance;
  
  constructor() {
    this.client = axios.create({
      baseURL: process.env.NEXT_PUBLIC_API_URL,
      headers: {
        'Content-Type': 'application/json',
      },
    });
    
    // Request interceptor to add auth token
    this.client.interceptors.request.use(
      (config) => {
        const token = localStorage.getItem('access_token');
        const storeId = localStorage.getItem('current_store_id');
        
        if (token) {
          config.headers.Authorization = `Bearer ${token}`;
        }
        
        if (storeId) {
          config.headers['X-Store-ID'] = storeId;
        }
        
        return config;
      },
      (error) => Promise.reject(error)
    );
    
    // Response interceptor for token refresh
    this.client.interceptors.response.use(
      (response) => response,
      async (error) => {
        const originalRequest = error.config;
        
        // If 401 and we haven't retried yet
        if (error.response?.status === 401 && !originalRequest._retry) {
          originalRequest._retry = true;
          
          try {
            const refreshToken = localStorage.getItem('refresh_token');
            const response = await axios.post(
              `${process.env.NEXT_PUBLIC_API_URL}/api/v1/auth/refresh/`,
              { refresh: refreshToken }
            );
            
            const { access } = response.data;
            localStorage.setItem('access_token', access);
            
            originalRequest.headers.Authorization = `Bearer ${access}`;
            return this.client(originalRequest);
          } catch (refreshError) {
            // Refresh failed, logout user
            localStorage.clear();
            window.location.href = '/login';
            return Promise.reject(refreshError);
          }
        }
        
        return Promise.reject(error);
      }
    );
  }
  
  async get<T>(url: string, config?: AxiosRequestConfig) {
    const response = await this.client.get<T>(url, config);
    return response.data;
  }
  
  async post<T>(url: string, data?: any, config?: AxiosRequestConfig) {
    const response = await this.client.post<T>(url, data, config);
    return response.data;
  }
  
  async put<T>(url: string, data?: any, config?: AxiosRequestConfig) {
    const response = await this.client.put<T>(url, data, config);
    return response.data;
  }
  
  async delete<T>(url: string, config?: AxiosRequestConfig) {
    const response = await this.client.delete<T>(url, config);
    return response.data;
  }
}

export const apiClient = new ApiClient();
```

**Auth Store (Zustand):**

```typescript
// store/authStore.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface User {
  id: string;
  email: string;
  full_name: string;
}

interface AuthState {
  user: User | null;
  accessToken: string | null;
  refreshToken: string | null;
  isAuthenticated: boolean;
  
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
  setUser: (user: User) => void;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      accessToken: null,
      refreshToken: null,
      isAuthenticated: false,
      
      login: async (email, password) => {
        const response = await apiClient.post('/api/v1/auth/login/', {
          email,
          password,
        });
        
        const { access, refresh, user } = response.data;
        
        localStorage.setItem('access_token', access);
        localStorage.setItem('refresh_token', refresh);
        
        set({
          user,
          accessToken: access,
          refreshToken: refresh,
          isAuthenticated: true,
        });
      },
      
      logout: () => {
        localStorage.clear();
        set({
          user: null,
          accessToken: null,
          refreshToken: null,
          isAuthenticated: false,
        });
      },
      
      setUser: (user) => set({ user }),
    }),
    {
      name: 'auth-storage',
    }
  )
);
```

**Login Page:**

```tsx
// app/(auth)/login/page.tsx
'use client';

import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import * as z from 'zod';
import { useAuthStore } from '@/store/authStore';
import { Button } from '@/components/ui/Button';
import { Input } from '@/components/ui/Input';

const loginSchema = z.object({
  email: z.string().email('Invalid email address'),
  password: z.string().min(6, 'Password must be at least 6 characters'),
});

type LoginForm = z.infer<typeof loginSchema>;

export default function LoginPage() {
  const router = useRouter();
  const { login } = useAuthStore();
  const [error, setError] = useState('');
  const [isLoading, setIsLoading] = useState(false);
  
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm<LoginForm>({
    resolver: zodResolver(loginSchema),
  });
  
  const onSubmit = async (data: LoginForm) => {
    setIsLoading(true);
    setError('');
    
    try {
      await login(data.email, data.password);
      router.push('/dashboard');
    } catch (err: any) {
      setError(err.response?.data?.message || 'Login failed');
    } finally {
      setIsLoading(false);
    }
  };
  
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
      <div className="max-w-md w-full space-y-8">
        <div>
          <h2 className="mt-6 text-center text-3xl font-extrabold text-gray-900">
            Sign in to SellPilot
          </h2>
        </div>
        
        <form className="mt-8 space-y-6" onSubmit={handleSubmit(onSubmit)}>
          {error && (
            <div className="rounded-md bg-red-50 p-4">
              <p className="text-sm text-red-800">{error}</p>
            </div>
          )}
          
          <div className="rounded-md shadow-sm -space-y-px">
            <div>
              <Input
                {...register('email')}
                type="email"
                placeholder="Email address"
                error={errors.email?.message}
              />
            </div>
            
            <div>
              <Input
                {...register('password')}
                type="password"
                placeholder="Password"
                error={errors.password?.message}
              />
            </div>
          </div>
          
          <div>
            <Button
              type="submit"
              className="w-full"
              loading={isLoading}
            >
              Sign in
            </Button>
          </div>
        </form>
      </div>
    </div>
  );
}
```

#### 3. Dashboard Layout

**Main Layout:**

```tsx
// app/(dashboard)/layout.tsx
'use client';

import { useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { useAuthStore } from '@/store/authStore';
import { Sidebar } from '@/components/layouts/Sidebar';
import { Header } from '@/components/layouts/Header';

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const router = useRouter();
  const { isAuthenticated } = useAuthStore();
  
  useEffect(() => {
    if (!isAuthenticated) {
      router.push('/login');
    }
  }, [isAuthenticated, router]);
  
  if (!isAuthenticated) {
    return null;
  }
  
  return (
    <div className="min-h-screen bg-gray-100">
      <Sidebar />
      
      <div className="lg:pl-64">
        <Header />
        
        <main className="py-6 px-4 sm:px-6 lg:px-8">
          {children}
        </main>
      </div>
    </div>
  );
}
```

**Sidebar Component:**

```tsx
// components/layouts/Sidebar.tsx
'use client';

import Link from 'next/link';
import { usePathname } from 'next/navigation';
import {
  HomeIcon,
  ShoppingBagIcon,
  ShoppingCartIcon,
  UserGroupIcon,
  ChartBarIcon,
  CogIcon,
} from '@heroicons/react/24/outline';
import { cn } from '@/lib/utils/cn';

const navigation = [
  { name: 'Dashboard', href: '/dashboard', icon: HomeIcon },
  { name: 'Products', href: '/dashboard/products', icon: ShoppingBagIcon },
  { name: 'Orders', href: '/dashboard/orders', icon: ShoppingCartIcon },
  { name: 'Customers', href: '/dashboard/customers', icon: UserGroupIcon },
  { name: 'Analytics', href: '/dashboard/analytics', icon: ChartBarIcon },
  { name: 'Settings', href: '/dashboard/settings', icon: CogIcon },
];

export function Sidebar() {
  const pathname = usePathname();
  
  return (
    <div className="hidden lg:fixed lg:inset-y-0 lg:flex lg:w-64 lg:flex-col">
      <div className="flex flex-col flex-grow bg-white border-r border-gray-200 pt-5 overflow-y-auto">
        <div className="flex items-center flex-shrink-0 px-4">
          <h1 className="text-2xl font-bold text-indigo-600">SellPilot</h1>
        </div>
        
        <div className="mt-5 flex-grow flex flex-col">
          <nav className="flex-1 px-2 space-y-1">
            {navigation.map((item) => {
              const isActive = pathname === item.href;
              
              return (
                <Link
                  key={item.name}
                  href={item.href}
                  className={cn(
                    'group flex items-center px-2 py-2 text-sm font-medium rounded-md',
                    isActive
                      ? 'bg-indigo-50 text-indigo-600'
                      : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'
                  )}
                >
                  <item.icon
                    className={cn(
                      'mr-3 flex-shrink-0 h-6 w-6',
                      isActive ? 'text-indigo-600' : 'text-gray-400'
                    )}
                    aria-hidden="true"
                  />
                  {item.name}
                </Link>
              );
            })}
          </nav>
        </div>
      </div>
    </div>
  );
}
```

#### 4. Products Management

**Products List Page:**

```tsx
// app/(dashboard)/products/page.tsx
'use client';

import { useState } from 'react';
import { useQuery } from '@tanstack/react-query';
import Link from 'next/link';
import { PlusIcon } from '@heroicons/react/24/outline';
import { Button } from '@/components/ui/Button';
import { Table } from '@/components/ui/Table';
import { Badge } from '@/components/ui/Badge';
import { formatCurrency } from '@/lib/utils/formatters';
import { apiClient } from '@/lib/api/client';

export default function ProductsPage() {
  const [page, setPage] = useState(1);
  
  const { data, isLoading } = useQuery({
    queryKey: ['products', page],
    queryFn: () => apiClient.get(`/api/v1/products/?page=${page}`),
  });
  
  const columns = [
    {
      header: 'Product',
      accessor: 'title',
      cell: (row: any) => (
        <div className="flex items-center">
          {row.primary_image && (
            <img
              src={row.primary_image}
              alt={row.title}
              className="h-10 w-10 rounded object-cover mr-3"
            />
          )}
          <div>
            <div className="font-medium text-gray-900">{row.title}</div>
            <div className="text-sm text-gray-500">{row.sku}</div>
          </div>
        </div>
      ),
    },
    {
      header: 'Status',
      accessor: 'status',
      cell: (row: any) => (
        <Badge
          variant={row.status === 'active' ? 'success' : 'gray'}
        >
          {row.status}
        </Badge>
      ),
    },
    {
      header: 'Price',
      accessor: 'base_price',
      cell: (row: any) => formatCurrency(row.base_price),
    },
    {
      header: 'Stock',
      accessor: 'total_stock',
      cell: (row: any) => (
        <span className={row.total_stock < 10 ? 'text-red-600' : ''}>
          {row.total_stock}
        </span>
      ),
    },
    {
      header: 'Variants',
      accessor: 'variant_count',
    },
  ];
  
  return (
    <div>
      <div className="sm:flex sm:items-center sm:justify-between mb-6">
        <div>
          <h1 className="text-2xl font-semibold text-gray-900">Products</h1>
          <p className="mt-2 text-sm text-gray-700">
            Manage your product catalog
          </p>
        </div>
        
        <div className="mt-4 sm:mt-0">
          <Link href="/dashboard/products/new">
            <Button>
              <PlusIcon className="h-5 w-5 mr-2" />
              Add Product
            </Button>
          </Link>
        </div>
      </div>
      
      <div className="bg-white shadow rounded-lg">
        <Table
          data={data?.data || []}
          columns={columns}
          loading={isLoading}
          onRowClick={(row) => router.push(`/dashboard/products/${row.id}`)}
        />
        
        {/* Pagination */}
        <div className="px-4 py-3 border-t border-gray-200">
          {/* Pagination component */}
        </div>
      </div>
    </div>
  );
}
```

#### 5. Analytics Dashboard

**Dashboard with Charts:**

```tsx
// app/(dashboard)/analytics/page.tsx
'use client';

import { useQuery } from '@tanstack/react-query';
import { Card } from '@/components/ui/Card';
import { RevenueChart } from '@/components/charts/RevenueChart';
import { OrdersChart } from '@/components/charts/OrdersChart';
import { formatCurrency } from '@/lib/utils/formatters';
import { apiClient } from '@/lib/api/client';

export default function AnalyticsPage() {
  const { data: overview } = useQuery({
    queryKey: ['analytics-overview'],
    queryFn: () => apiClient.get('/api/v1/analytics/overview/?period=30d'),
  });
  
  const stats = [
    {
      name: 'Total Revenue',
      value: formatCurrency(overview?.data?.revenue?.current || 0),
      change: overview?.data?.revenue?.change_percent,
    },
    {
      name: 'Orders',
      value: overview?.data?.orders?.current || 0,
      change: overview?.data?.orders?.change_percent,
    },
    {
      name: 'Avg Order Value',
      value: formatCurrency(overview?.data?.avg_order_value?.current || 0),
      change: overview?.data?.avg_order_value?.change_percent,
    },
  ];
  
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-semibold text-gray-900">Analytics</h1>
      
      {/* Stats Cards */}
      <div className="grid grid-cols-1 gap-5 sm:grid-cols-3">
        {stats.map((stat) => (
          <Card key={stat.name}>
            <div className="px-4 py-5 sm:p-6">
              <dt className="text-sm font-medium text-gray-500 truncate">
                {stat.name}
              </dt>
              <dd className="mt-1 flex items-baseline justify-between">
                <div className="text-3xl font-semibold text-gray-900">
                  {stat.value}
                </div>
                {stat.change !== undefined && (
                  <div className={cn(
                    'text-sm font-medium',
                    stat.change >= 0 ? 'text-green-600' : 'text-red-600'
                  )}>
                    {stat.change >= 0 ? '+' : ''}{stat.change.toFixed(1)}%
                  </div>
                )}
              </dd>
            </div>
          </Card>
        ))}
      </div>
      
      {/* Charts */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <Card>
          <div className="px-4 py-5 sm:p-6">
            <h3 className="text-lg font-medium text-gray-900 mb-4">
              Revenue Trend
            </h3>
            <RevenueChart />
          </div>
        </Card>
        
        <Card>
          <div className="px-4 py-5 sm:p-6">
            <h3 className="text-lg font-medium text-gray-900 mb-4">
              Orders Over Time
            </h3>
            <OrdersChart />
          </div>
        </Card>
      </div>
    </div>
  );
}
```

### Testing Strategy

**Component Tests:**

```typescript
// components/ui/__tests__/Button.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { Button } from '../Button';

describe('Button', () => {
  it('renders children correctly', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });
  
  it('handles click events', () => {
    const handleClick = jest.fn();
    render(<Button onClick={handleClick}>Click me</Button>);
    
    fireEvent.click(screen.getByText('Click me'));
    expect(handleClick).toHaveBeenCalledTimes(1);
  });
  
  it('shows loading state', () => {
    render(<Button loading>Click me</Button>);
    expect(screen.getByRole('button')).toBeDisabled();
  });
});
```

**E2E Tests (Playwright):**

```typescript
// tests/e2e/login.spec.ts
import { test, expect } from '@playwright/test';

test('user can login successfully', async ({ page }) => {
  await page.goto('/login');
  
  await page.fill('input[name="email"]', 'test@example.com');
  await page.fill('input[name="password"]', 'password123');
  await page.click('button[type="submit"]');
  
  await expect(page).toHaveURL('/dashboard');
  await expect(page.locator('h1')).toContainText('Dashboard');
});
```

### Acceptance Criteria

Phase 4 is complete when:

- [ ] Authentication flow fully functional (login, logout, token refresh)
- [ ] Dashboard layout responsive and polished
- [ ] Products CRUD interface complete
- [ ] Orders list and detail pages working
- [ ] Customers management interface functional
- [ ] Analytics dashboard with working charts
- [ ] Settings pages implemented
- [ ] All forms have proper validation
- [ ] Loading states implemented everywhere
- [ ] Error handling comprehensive
- [ ] Mobile responsive (works on tablets and phones)
- [ ] TypeScript strict mode with no errors
- [ ] Component tests written for UI components
- [ ] E2E tests for critical flows
- [ ] Accessibility standards met (WCAG 2.1 AA)

### Success Metrics

- First contentful paint < 1.5s
- Time to interactive < 3s
- Lighthouse score > 90
- Zero console errors
- Works on Chrome, Firefox, Safari
- Mobile usable (can perform core tasks on phone)

---

*[Due to length, continuing with Phases 5-8 and Appendix...]*

