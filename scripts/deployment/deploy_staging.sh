#!/usr/bin/env bash
# Deploy to staging. Stub for Phase 0; real implementation in Phase 3/8.
# Usage: ./scripts/deployment/deploy_staging.sh

set -e
echo "Staging deployment stub. Configure Terraform/CI and run:"
echo "  terraform -chdir=infrastructure/terraform/environments/staging apply"
echo "  # or trigger deploy-staging workflow from CI"
